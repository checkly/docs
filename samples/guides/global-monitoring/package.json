{
  "name": "global-monitoring",
  "private": true,
  "description": "Sample project for the Checkly guide: Why monitoring from around the globe is critical",
  "scripts": {
    "checkly:test": "checkly test",
    "checkly:deploy": "checkly deploy"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "checkly": "^9.5.0"
  }
}
