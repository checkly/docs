{
  "name": "checkout-flow",
  "private": true,
  "description": "Sample project for the Checkly guide: Monitor a checkout flow",
  "scripts": {
    "test": "playwright test",
    "checkly:test": "checkly test",
    "checkly:deploy": "checkly deploy"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "checkly": "^9.5.0"
  }
}
