---
title: 'PhoneCallAlertChannel Construct'
description: 'Learn how to configure phone call alert channels with the Checkly CLI.'
sidebarTitle: 'Phone Call Alert Channel'
---

Use PhoneCallAlertChannel to make voice calls when checks fail or recover. Phone call alerts are the most immediate form of notification, ideal for critical systems requiring instant attention.

<Info>
Learn more about phone call alerts in the [Phone Calls documentation](/docs/communicate/alerts/channels).
</Info>

## `PhoneCallAlertChannel` Structure

<CodeGroup>

```ts Basic Example
import { PhoneCallAlertChannel } from 'checkly/constructs'

const callChannel = new PhoneCallAlertChannel('call-channel-1', {
  phoneNumber: '+1234567890'
})
```

```ts Advanced Example
import { PhoneCallAlertChannel } from 'checkly/constructs'

const emergencyCall = new PhoneCallAlertChannel('emergency-call-channel', {
  phoneNumber: '+1234567890',
  sendRecovery: false, // Only call for failures, not recoveries
  sendFailure: true,
  sendDegrade: false,  // Only for complete failures
  sslExpiry: true,
  sslExpiryThreshold: 3 // Call 3 days before SSL expires
})
```

</CodeGroup>

## `PhoneCallAlertChannel` Configuration

<Tabs>
  <Tab title="Unique Configuration">

Configure phone call-specific settings:

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `phoneNumber` | `string` | ✅ | - | Phone number in international format (e.g., +1234567890) |

</Tab>

<Tab title="Shared Configuration">
Configure common alert channel properties:

| Property | Type | Required | Default | Description |
|----------|------|----------|---------|-------------|
| `sendRecovery` | `boolean` | ❌ | `true` | Make calls when checks recover |
| `sendFailure` | `boolean` | ❌ | `true` | Make calls when checks fail |
| `sendDegrade` | `boolean` | ❌ | `false` | Make calls when checks degrade (API checks only) |
| `sslExpiry` | `boolean` | ❌ | `false` | Make calls for SSL certificate expiry |
| `sslExpiryThreshold` | `number` | ❌ | `30` | Days before SSL expiry to make call |

</Tab>
</Tabs>

## Phone Call Alert Channel Examples

<Tabs>
  <Tab title="Emergency Response">
    ```ts
    import { PhoneCallAlertChannel, ApiCheck } from 'checkly/constructs'

    const emergencyCall = new PhoneCallAlertChannel('emergency-response', {
      phoneNumber: '+1555EMERGENCY',
      sendFailure: true,
      sendRecovery: false, // Don't call for recoveries
      sendDegrade: false   // Only complete failures
    })

    new ApiCheck('life-safety-system', {
      name: 'Life Safety System',
      alertChannels: [emergencyCall],
      tags: ['life-safety', 'critical'],
      request: {
        method: 'GET',
        url: 'https://life-safety.example.com/health'
      }
    })
    ```
  </Tab>
  
  <Tab title="Executive Notifications">
    ```ts
    const executiveCall = new PhoneCallAlertChannel('executive-call', {
      phoneNumber: '+1555EXEC', // Executive phone number
      sendFailure: true,
      sendRecovery: true, // Notify of both failures and recoveries
      sendDegrade: false
    })

    new ApiCheck('revenue-critical-api', {
      name: 'Revenue Critical API',
      alertChannels: [executiveCall],
      tags: ['revenue-critical', 'executive-level'],
      request: {
        method: 'GET',
        url: 'https://payments.example.com/health'
      }
    })
    ```
  </Tab>
  
  <Tab title="On-Call Escalation">
    ```ts
    const primaryCall = new PhoneCallAlertChannel('primary-oncall-call', {
      phoneNumber: '+1555PRIMARY'
    })

    const secondaryCall = new PhoneCallAlertChannel('secondary-oncall-call', {
      phoneNumber: '+1555SECONDARY'
    })

    const managerCall = new PhoneCallAlertChannel('manager-call', {
      phoneNumber: '+1555MANAGER',
      sendFailure: true,
      sendRecovery: false // Manager only needs to know about failures
    })

    new ApiCheck('escalation-api-check', {
      name: 'Critical System with Escalation',
      alertChannels: [primaryCall, secondaryCall, managerCall],
      tags: ['critical', 'escalation'],
      request: {
        method: 'GET',
        url: 'https://critical.example.com/health'
      }
    })
    ```
  </Tab>
  
  <Tab title="Security Incident Response">
    ```ts
    const securityCall = new PhoneCallAlertChannel('security-incident', {
      phoneNumber: '+1555SECURITY',
      sendFailure: true,
      sendRecovery: false,
      sslExpiry: true,
      sslExpiryThreshold: 1 // Call immediately for SSL expiry
    })

    new ApiCheck('security-endpoint', {
      name: 'Security Endpoint Monitor',
      alertChannels: [securityCall],
      tags: ['security', 'authentication'],
      request: {
        method: 'GET',
        url: 'https://auth.example.com/security-health'
      }
    })
    ```
  </Tab>
  
  <Tab title="Financial Services">
    ```ts
    const tradingCall = new PhoneCallAlertChannel('trading-desk-call', {
      phoneNumber: '+1555TRADING'
    })

    const riskCall = new PhoneCallAlertChannel('risk-management-call', {
      phoneNumber: '+1555RISK'
    })

    // Trading system monitoring
    new ApiCheck('trading-system', {
      name: 'Trading System Health',
      alertChannels: [tradingCall],
      tags: ['trading', 'financial'],
      request: {
        method: 'GET',
        url: 'https://trading.example.com/health'
      }
    })

    // Risk management system
    new ApiCheck('risk-system', {
      name: 'Risk Management System',
      alertChannels: [riskCall],
      tags: ['risk', 'financial'],
      request: {
        method: 'GET',
        url: 'https://risk.example.com/health'
      }
    })
    ```
  </Tab>
  
  <Tab title="Healthcare Systems">
    ```ts
    const medicalCall = new PhoneCallAlertChannel('medical-oncall', {
      phoneNumber: '+1555MEDICAL',
      sendFailure: true,
      sendRecovery: true, // Important to know when systems recover
      sendDegrade: true   // Even degraded performance matters in healthcare
    })

    new ApiCheck('patient-records-system', {
      name: 'Patient Records System',
      alertChannels: [medicalCall],
      tags: ['healthcare', 'patient-records'],
      request: {
        method: 'GET',
        url: 'https://records.hospital.com/health'
      }
    })

    new ApiCheck('medical-devices-api', {
      name: 'Medical Devices API',
      alertChannels: [medicalCall],
      tags: ['healthcare', 'medical-devices'],
      maxResponseTime: 2000, // Very strict performance requirements
      degradedResponseTime: 1000,
      request: {
        method: 'GET',
        url: 'https://devices.hospital.com/health'
      }
    })
    ```
  </Tab>
</Tabs>

## Phone Number Format

Phone call alert channels require phone numbers in international format:

<Tabs>
  <Tab title="Correct Format">
    ```ts
    // US numbers
    const usCall = new PhoneCallAlertChannel('us-call', {
      phoneNumber: '+15551234567' // +1 (country code) + area code + number
    })

    // UK numbers
    const ukCall = new PhoneCallAlertChannel('uk-call', {
      phoneNumber: '+447911123456' // +44 (country code) + mobile number
    })

    // German numbers
    const deCall = new PhoneCallAlertChannel('de-call', {
      phoneNumber: '+491701234567' // +49 (country code) + mobile number
    })

    // Australian numbers
    const auCall = new PhoneCallAlertChannel('au-call', {
      phoneNumber: '+61412345678' // +61 (country code) + mobile number
    })
    ```
  </Tab>
  
  <Tab title="Incorrect Format">
    ```ts
    // ❌ Don't use these formats:
    
    // Missing country code
    phoneNumber: '5551234567'
    
    // With spaces or dashes
    phoneNumber: '+1 555-123-4567'
    
    // With parentheses
    phoneNumber: '+1 (555) 123-4567'
    
    // Domestic format
    phoneNumber: '(555) 123-4567'
    ```
  </Tab>
</Tabs>

## Combining with Other Alert Methods

Phone calls should be used as part of a comprehensive alerting strategy:

```ts
import { PhoneCallAlertChannel, SmsAlertChannel, EmailAlertChannel, SlackAlertChannel } from 'checkly/constructs'

// Layered alerting approach
const teamSlack = new SlackAlertChannel('team-slack', {
  url: new URL('https://hooks.slack.com/services/YOUR/WEBHOOK/URL'),
  channel: '#alerts'
})

const oncallEmail = new EmailAlertChannel('oncall-email', {
  address: 'oncall@example.com'
})

const oncallSms = new SmsAlertChannel('oncall-sms', {
  phoneNumber: '+1555ONCALL'
})

const emergencyCall = new PhoneCallAlertChannel('emergency-call', {
  phoneNumber: '+1555EMERGENCY',
  sendFailure: true,
  sendRecovery: false // Only call for failures
})

new ApiCheck('comprehensive-alerting', {
  name: 'Critical System with Comprehensive Alerting',
  alertChannels: [
    teamSlack,      // Immediate team visibility
    oncallEmail,    // Documentation and detail
    oncallSms,      // Mobile notification
    emergencyCall   // Immediate voice alert
  ],
  tags: ['critical', 'comprehensive'],
  request: {
    method: 'GET',
    url: 'https://critical.example.com/health'
  }
})
```

## Best Practices

<Warning>
**Use Sparingly**: Phone calls are the most intrusive form of notification. Reserve them for truly critical systems where immediate human intervention is required.
</Warning>

<Info>
**Recovery Notifications**: Consider whether recovery calls are necessary. For some critical systems, knowing when the system recovers is as important as knowing when it fails.
</Info>

<Tabs>
  <Tab title="Critical Systems Only">
    ```ts
    // Only use calls for systems where immediate response is critical
    const criticalCall = new PhoneCallAlertChannel('critical-only', {
      phoneNumber: '+1555CRITICAL',
      sendFailure: true,
      sendRecovery: false, // Reduce call frequency
      sendDegrade: false   // Only complete failures
    })

    new ApiCheck('mission-critical-system', {
      name: 'Mission Critical System',
      alertChannels: [criticalCall],
      tags: ['mission-critical', 'immediate-response'],
      request: {
        method: 'GET',
        url: 'https://mission-critical.example.com/health'
      }
    })
    ```
  </Tab>
  
  <Tab title="Time-Based Alerting">
    ```ts
    // Different alerting during business hours vs. off-hours
    const businessHoursCall = new PhoneCallAlertChannel('business-hours', {
      phoneNumber: '+1555BUSINESS'
    })

    const afterHoursCall = new PhoneCallAlertChannel('after-hours', {
      phoneNumber: '+1555AFTERHOURS'
    })

    // You might use different checks or logic to determine which channel to use
    // based on time of day or other factors
    ```
  </Tab>
  
  <Tab title="Gradual Escalation">
    ```ts
    // Start with less intrusive methods, escalate to calls
    const teamEmail = new EmailAlertChannel('team-email', {
      address: 'team@example.com'
    })

    const urgentSms = new SmsAlertChannel('urgent-sms', {
      phoneNumber: '+1555SMS'
    })

    const emergencyCall = new PhoneCallAlertChannel('emergency-call', {
      phoneNumber: '+1555CALL'
    })

    // Less critical: email only
    new ApiCheck('standard-service', {
      name: 'Standard Service',
      alertChannels: [teamEmail],
      request: {
        method: 'GET',
        url: 'https://api.example.com/standard'
      }
    })

    // Important: email + SMS
    new ApiCheck('important-service', {
      name: 'Important Service',
      alertChannels: [teamEmail, urgentSms],
      request: {
        method: 'GET',
        url: 'https://api.example.com/important'
      }
    })

    // Critical: all methods including phone call
    new ApiCheck('critical-service', {
      name: 'Critical Service',
      alertChannels: [teamEmail, urgentSms, emergencyCall],
      request: {
        method: 'GET',
        url: 'https://api.example.com/critical'
      }
    })
    ```
  </Tab>
</Tabs>