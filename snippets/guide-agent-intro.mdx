Paste the prompt below into Claude Code, Cursor, Codex, or any coding agent with [Checkly Skills](/ai/skills) installed. It builds the same setup as this guide, proves it with `npx checkly test --record`, and stops for your confirmation before `npx checkly deploy`.
