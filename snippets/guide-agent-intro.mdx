If you ran `npx checkly init` in the [quickstart](/quickstart), your coding agent already has [Checkly Skills](/ai/skills) installed. Paste the prompt below into Claude Code, Cursor, Codex, or any agent that supports skills. It builds the same setup as this guide, proves it with `npx checkly test --record`, and stops for your confirmation before `npx checkly deploy`.
